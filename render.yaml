services:
  # Unified App (Backend + Frontend)
  - type: web
    name: travel-concierge
    env: docker
    region: oregon
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 8001
    healthCheckPath: /health
